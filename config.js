/* =========================================================
   YuzuTube 接続設定（Vercelにデプロイする前に1度だけ編集）
   ---------------------------------------------------------
   Termux側の publish.sh が、この Gist に現在のサーバーURLを
   自動で書き込む。サイトは起動時にこの Gist を読んで自動接続する。

   手順:
   1) GitHubで空のGistを1つ作る（中身は何でもよい）
   2) そのURLの末尾のID（例 .../gists/ の後の英数字）を gistId に貼る
   3) 同じIDを Termux の .env の GIST_ID にも設定する
   ========================================================= */
window.YUZU_CONFIG = {
  gistId: "",                     // 例: "a1b2c3d4e5f60718293a4b5c6d7e8f90"
  fileName: "yuzu_server.json"
};
